function vehicleType ( color , object) {
    if (object == 2) {
        console.log ("a " + color + " motobike" );
    }
    else {
        console.log ("a " + color + " car");

    }
}
vehicleType("blue", 2)