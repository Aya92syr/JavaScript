function vehicleType ( color , object, age ) {
    if (object == 1 && age == 5) {
        console.log ("a " + color + " used " + "car" );
    }
    else {
        console.log ("a " + color + "new" + " car");

    }
}
vehicleType("blue", 1, 5)